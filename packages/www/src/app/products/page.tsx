import { Box } from "@mui/material";

import { AppLayout } from "@/components/app-layout";

export default async function ProductsPage() {
  return (
    <AppLayout>
      <Box maxWidth={600} margin="0 auto" pt={2}></Box>
    </AppLayout>
  );
}
